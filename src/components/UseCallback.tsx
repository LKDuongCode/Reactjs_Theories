import React from 'react'

export default function UseCallback() {
  return (
    <div>
      {/* 
      - Phương thức bên trong nhận vào một callback là ()=>{} và một [] hoặc [depend]
      ()=>{} có nhiệm vụ đi xử lí logic.
      khi nào depend thay đổi thi useCallback được gọi. 
      useCallBack(()=>{

      },[])
      -  Đây là một hook trong react.
      - cách dùng  => import.
      - công dụng: -
      */}
    </div>
  )
}
